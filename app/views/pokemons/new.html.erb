<h1> Create a Task! </h1>
<%= simple_form_for Pokemon.new, url: {action: "create"} do |f| %>
  Task: <%= f.input :name %>
  <%= f.button :submit, 'Create Task!' %>
<% end %>

<p>List of Recently Deleted Tasks:</p>
  	<% Pokemon.only_deleted.each do |pokemon| %>
	    <li><%= pokemon.name %></li>
	<% end %>
